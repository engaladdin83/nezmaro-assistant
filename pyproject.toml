[project]
name = "nezmaro_assistant"
authors = [{ name = "ASMY Co.", email = "info@hdgcommunity.com" }]
description = "Ask Nezmaro: the in-ERP assistant button and panel for Nezmaro tenant sites"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<16.0.0"
